name: 'manifest-generator'
description: ''
author: ''
inputs:
  path: # change this
    description: ''
    default: 'dist'
runs:
  using: 'composite' # 'node12'
  steps: 
    - name: Manifest
      run: npm run package.json && node scripts/manifest
      shell: bash
