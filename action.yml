name: 'manifest-generator'
description: ''
author: 'wiz417'
inputs:
  path: # change this
    required: true
    description: 'directory for collecting filenames'
    default: 'dist'
runs:
  using: 'node12'
  main: 'dist/index.js'
